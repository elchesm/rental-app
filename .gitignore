# ========================
# 동거동락 임대관리앱 .gitignore
# ========================

# OS 파일
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# 백업 / 내보내기 파일 (개인정보 포함 가능)
*.json
*.csv
임대관리앱_*.csv
임대관리앱_*.json

# 에디터 설정
.vscode/
.idea/
*.swp
*.swo

# 로컬 환경 설정 파일
.env
.env.local
config.local.js

# 주의: rent_app.html 안의 FIREBASE_CONFIG 값은
# 이 파일이 private 저장소에 있을 때만 안전합니다.
# 절대 public 저장소로 전환하지 마세요.

# Firebase local deploy cache
.firebase/

# 하위 디렉터리의 stale git repo (배포 아티팩트)
Rental-App/
